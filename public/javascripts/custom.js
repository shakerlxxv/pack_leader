
$(function() {
        $( "#user_submit" ).button({
            icons: {
                       primary: "ui-icon-locked"
                   }, text: true
        });

        $( "#test_button" ).button({
            icons: { primary: "ui-icon-locked" },
            text: true
        });
});
