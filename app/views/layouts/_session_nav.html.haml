#session_nav
  %a{:href=>root_path}
    = image_tag "pack_leader_logo.gif", :class => 'logo_img'
  - if current_user 
    Logged in as
    = current_user.name
    = link_to "Log out", log_out_path
  - else
    = link_to "Sign up", sign_up_path
    or
    = link_to "Log in", log_in_path
